---
description: Project-specific standards and architecture decisions
alwaysApply: true
---

# Project Standards

> **Philosophy**: Only document what differs from common conventions. Agent already knows SOLID, REST, React patterns, etc. Focus on YOUR project's unique decisions.

## Architecture

<!-- 
FILL IN: Your specific architecture decisions
Example: "We use feature-based folders, not layer-based"
-->

### Folder Structure

```
src/
  {{DESCRIBE_YOUR_STRUCTURE}}
```

### Key Decisions

- **State Management**: {{YOUR_APPROACH}} — because {{REASON}}
- **API Pattern**: {{YOUR_PATTERN}} — because {{REASON}}
- **Error Handling**: {{YOUR_APPROACH}} — because {{REASON}}

## Conventions

<!-- 
FILL IN: Only conventions that DIFFER from defaults
Don't include: naming conventions Agent already knows, common patterns, etc.
-->

### Naming

- {{CUSTOM_CONVENTION_1}}
- {{CUSTOM_CONVENTION_2}}

### Patterns We Use

- {{PATTERN_1}}: {{BRIEF_EXPLANATION}}
- {{PATTERN_2}}: {{BRIEF_EXPLANATION}}

### Patterns We Avoid

- {{ANTI_PATTERN}}: {{WHY_WE_AVOID_IT}}

## Internal Libraries & Services

<!-- 
FILL IN: Internal tools Agent won't know about
-->

- **Auth**: Use `@/lib/auth` for all authentication — see @lib/auth/README.md
- **API Client**: Use `@/lib/api` wrapper, not raw fetch
- **Components**: Import from `@/components/ui` for design system

## Security Requirements

<!-- 
FILL IN: Project-specific security rules
-->

- All PII fields must use `encryptField()` before storage
- API keys go in `.env.local`, never in code
- {{ADDITIONAL_SECURITY_REQUIREMENT}}

## External References

<!-- Link to canonical examples instead of copying code -->

- Component patterns: @src/components/Button.tsx
- API route pattern: @src/app/api/users/route.ts
- Test pattern: @src/lib/__tests__/example.test.ts
